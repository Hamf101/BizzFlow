# Dependencies
node_modules/

# Next.js
.next/
out/
tsconfig.tsbuildinfo

# Production builds
dist/
build/

# Environment files
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local tooling
.turbo/
.vercel/
.supabase/
supabase/.temp/

# Internal material — never committed, kept locally only.
# Agent guides, planning notes, spikes, audits, threat models, and
# verification evidence. None of it is meaningful to an outside reader.
.agent/
.agents/
AGENTS.md
.planning/
artifacts/
skills-lock.json

# Tests and coverage
coverage/
test-results/
playwright-report/
blob-report/
.playwright-cli/
output/playwright/
# Signed-in browser state for the seeded E2E roles. Regenerated by the setup
# project on every run and holds live session cookies — never commit it.
e2e/.auth/

# Temporary files
.tmp/
tmp/
temp/

# OS files
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# ZCode local session directory
.zcode/
